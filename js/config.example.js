/**
 * config.js
 * Fill these in after creating your Supabase project
 * (Project Settings → API → Project URL / anon public key).
 * The anon key is meant to be public/client-side — that's normal for Supabase.
 *
 * Copy this file to config.js (gitignored) and fill in your real values.
 * For Vercel deploys, see the README section on generating config.js from
 * environment variables at build time.
 */
const SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';

// Passcode for the app lock screen. Change this to whatever you like.
const APP_PASSCODE = 'pick-something-only-you-know';
